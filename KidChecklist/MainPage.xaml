<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:local="clr-namespace:KidChecklist"
             x:Class="KidChecklist.MainPage">

    <ContentPage.BindingContext>
        <local:MainViewModel/>
    </ContentPage.BindingContext>

    <ListView ItemsSource="{Binding Items}" SelectionMode="None">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <FlexLayout JustifyContent="Start" AlignItems="Center">
                        <CheckBox x:Name="CheckBox">
                            <CheckBox.Behaviors>
                                <mct:EventToCommandBehavior
                                    EventName="CheckedChanged"
                                    Command="{Binding CheckCommand}"
                                    CommandParameter="{Binding Path=IsChecked, Source={x:Reference CheckBox}}" />
                            </CheckBox.Behaviors>
                        </CheckBox>
                        <Label Text="{Binding Title}"/>
                    </FlexLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>
